<%= turbo_frame_tag dom_id(@story, :comments) do %>
  <% @comments.each do |comment| %>
    <%= turbo_frame_tag dom_id(comment, :wrapper), src: comment_path(comment, story_id: @story.id) %>
  <% end %>
<% end %>
