<%= turbo_stream.replace(dom_id_for_records(@commentable, @comment),
  partial: 'comments/comment', locals: { commentable: @commentable, comment: @comment })
%>
<%= turbo_stream.prepend "flash", partial: "shared/flash" %>
