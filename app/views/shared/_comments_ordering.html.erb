<%= form_tag(public_send(current_path, local_assigns[:params]), method: :get, class: 'mt-3') do %>
  <%= select_tag(:order_by, options_for_select(%i[new top].map { |filter| [t(filter), filter] }, [t(session[:comments_order_by]), session[:comments_order_by]]), onchange: 'this.form.submit();') %>
<% end %>
