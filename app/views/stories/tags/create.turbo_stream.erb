<% if @new_tag # if @tag.valid? %>
  <%= turbo_stream.append "#{dom_id(@story)}_form_tags" do %>
    <%= render partial: 'stories/tags/text_field', locals: { story: @story, tag: @tag } %>
  <% end %>
<% end %>

<%= turbo_stream.replace "#{dom_id(@story)}_new_tag" do %>
  <%= render partial: "stories/tags/form", locals: { story: @story, tag: @new_tag || @tag } %>
<% end %>

<%= turbo_stream.prepend('flash', partial: 'shared/flash') %>
