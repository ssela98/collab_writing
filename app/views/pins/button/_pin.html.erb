<%= tag.div id: "#{dom_id(comment)}_pin" do %>
  <%= button_to t('pin'), pins_path(pin: { story_id: comment.story_id, comment_id: comment.id }), data: { action: 'click->pin-comment#toggleClass' }, class: 'btn btn-link' %>
<% end %>