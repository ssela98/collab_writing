<%= tag.div id: "#{dom_id(comment)}_pin", data: { controller: "turbo-frame-visibility", turbo_frame_visibility_target: "hideableTurboFrame", username_value: "#{comment.story.user.username}" }, class: "d-none" do %>
  <%= button_to 'Unpin', pin_path(comment.pin), data: { action: 'click->pin-comment#toggleClass' }, class: "btn btn-link", method: :delete %>
<% end %>
