<%= tag.div id: "#{dom_id(comment)}_pin" do %>
  <%= button_to t('unpin'), pin_path(comment.pin), data: { action: 'click->pin-comment#toggleClass' }, class: 'btn btn-link', method: :delete %>
<% end %>
