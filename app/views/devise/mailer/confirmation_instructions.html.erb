<p><%= t('users.mailer.welcome', email: @email) %></p>

<p><%= t('users.mailer.content') %></p>

<p><%= link_to t('users.mailer.button'), confirmation_url(@resource, confirmation_token: @token) %></p>
