<p><%= t('users.mailer.confirmation.welcome', email: @email) %></p>

<p><%= t('users.mailer.confirmation.content') %></p>

<p><%= link_to t('users.mailer.confirmation.button'), confirmation_url(@resource, confirmation_token: @token) %></p>
